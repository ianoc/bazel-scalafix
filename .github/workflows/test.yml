name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run bazel build all
      run: ./bazel build //...
    - name: Run bazel test all
      run: |
        ./bazel test //...

